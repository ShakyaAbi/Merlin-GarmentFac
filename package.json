{
  "name": "merlin-lite-root",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm.cmd --workspace apps/web run dev",
    "dev:api": "npm.cmd --workspace apps/api run dev",
    "dev": "concurrently -n \"api,web\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm.cmd run dev:api\" \"npm.cmd run dev:web\"",
    "build:web": "npm.cmd --workspace apps/web run build",
    "build:api": "npm.cmd --workspace apps/api run build",
    "build": "npm.cmd run build:web && npm.cmd run build:api",
    "test:api": "npm.cmd --workspace apps/api run test"
  },
  "devDependencies": {
    "@types/papaparse": "^5.5.2",
    "concurrently": "^9.2.1",
    "tsx": "^4.21.0"
  },
  "dependencies": {
    "@playwright/mcp": "^0.0.68"
  },
  "prisma": {
    "seed": "tsx apps/api/prisma/seed.ts"
  }
}
